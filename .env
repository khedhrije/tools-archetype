# ---------------------------
# Application metadata
# ---------------------------

# Application version (used at build time with -ldflags)
VERSION=1.0.0

# Git commit hash (set in CI/CD, fallback to "unknown" if not set)
REVISION=unknown

# Build timestamp (UTC, set in CI/CD)
BUILT_AT=unknown

# ---------------------------
# Container registry settings
# ---------------------------

# Docker registry (Azure Container Registry or Docker Hub)
REPO=docker.io/khedhrije

# Target build platform
PLATFORM=linux/amd64

# Image name
IMAGE_NAME=archetype

# ---------------------------
# Kubernetes settings
# ---------------------------

# Namespace where the app will be deployed
KUBE_NAMESPACE=archetype-dev

# Directory with Kubernetes manifests
DEPLOYMENT_DIR=deployment/dev

# ---------------------------
# Application runtime
# ---------------------------

# Port the app listens on inside the container
APP_PORT=8080

# ---------------------------
# Docker Registry credentials (should be defined in .env, not hardcoded here)
# ---------------------------

DOCKER_SERVER=https://index.docker.io/v1/
DOCKER_USERNAME=khedhrije
DOCKER_PASSWORD=khQQlapW1@
DOCKER_EMAIL=khedhrije@gmail.com
